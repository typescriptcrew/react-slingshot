import * as chai from 'chai';

const assert = chai.assert;

describe('WHEN the dummy test runs', () => {
    it('SHOULD assert true', () => {
      // arrange

      // act

      // assert
      assert(true, `it's true!`);
    });
});
